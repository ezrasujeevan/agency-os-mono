import React from 'react'
import {Unstable_Grid2 as Grid} from '@mui/material'

interface RenderActionCellProps {

}

const RenderActionCell: React.FC<RenderActionCellProps> = ({}:RenderActionCellProps) => {
  return (<Grid container><Grid xs={12}>RenderActionCell</Grid></Grid>)
}

export default RenderActionCell